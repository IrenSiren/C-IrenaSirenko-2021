// Example program
#include <math.h>
#include <iostream>
#include <string>
using namespace std;
int main() {
    int a,b;
    double c;
    cout << "Write length of leg a" << endl;
    cin >> a;
    cout << "Write length of leg b" << endl;
    cin >> b;
    a = pow(a,2);
    b= pow(b,2);
    c = sqrt(a+b);
    cout << c << endl;
    return 0;


}